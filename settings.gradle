dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) // Разрешает локальные репозитории в модулях
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Current Activity"
include(":app")